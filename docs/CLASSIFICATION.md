# Classification

Status: ship

Rationale: the MVP is local-first, has deterministic fixtures, includes a reusable `SKILL.md`, and exposes both CLI and library APIs. It is ready for public trial by another agent builder.

Known follow-ups:

- Add configurable scoring rubrics.
- Add multi-post comparison.
- Add redaction checks for sensitive candidate data.
